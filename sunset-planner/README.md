# Sunset AV Scenario Planner

This tool helps analyze whether to hold & rent or sell a property.

## Setup

```bash
npm install
npm run dev
```

Use `npm run build` then `npm run preview` to test the production build.
